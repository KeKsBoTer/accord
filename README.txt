https://github.com/KeKsBoTer/accord
branch viz

Instructions:

Download Cargo or Rustup, and execute 
$ cargo build
where cargo is the path to cargo bin.

execute python script run_on_cluster passing 
the number of nodes as argument (up to now)

execute python script testclient passing any node:portnumber, 
visible on run_on_cluster execution.